[project]
name = "tiny-repo"
version = "0.0.0"
description = "REPO-HEAL fixture: a tiny Python package with a deliberate import cycle."
requires-python = ">=3.10"

[tool.poetry]
name = "tiny-repo"
version = "0.0.0"
